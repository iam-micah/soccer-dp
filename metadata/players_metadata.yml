version: 1
schema: public
tables:
    players:
        primary_key: player_id
        columns:
            player_id:
                type: integer
            first_name:
                type: string
            last_name:
                type: string
            country_of_birth:
                type: string
            height_in_cm:
                type: integer
